{% extends 'base.html.twig' %}

{% block title %}New Abonnement{% endblock %}

{% block body %}
     <div class="flex justify-center items-center min-h-screen">
        <div class="bg-white w-full max-w-md mx-auto rounded-xl shadow-lg p-8">
            <h2 class="text-2xl font-bold text-center mb-6">Ajouter un Pack</h2>

     {{ include('abonnement/_form.html.twig', {'button_label': 'Ajouter le pack'}) }}

 
    </div>
    </div>
       <a href="{{ path('app_abonnement_index') }}">back to list</a>
{% endblock %}
