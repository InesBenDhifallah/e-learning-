{% extends 'question2/layout.html.twig' %}

{% block content %}
    <h1 class="text-2xl font-bold mb-4">Ajouter une suggestion</h1>

    <div class="bg-white p-6 shadow-lg rounded-lg">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="bg-blue-500 text-white px-4 py-2 rounded-lg mt-4">{{ button_label|default('Enregistrer') }}</button>
        {{ form_end(form) }}
    </div>

    <div class="mt-4">
        <a href="{{ path('app_question2_show', {'id': question_id, 'quizz_id': quizz_id}) }}" class="bg-gray-500 text-white px-4 py-2 rounded-lg">
            Retour à la question
        </a>
    </div>
{% endblock %}
