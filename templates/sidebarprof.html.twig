<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Interface Professeur</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/1.6.5/flowbite.min.css">
</head>
<body class="flex bg-gray-100">
    <!-- Sidebar -->
    <aside class="w-64 bg-lime-500 text-white p-5 min-h-screen">
        <div class="mb-5">
            <img src="{{ asset('images/alpha.png') }}" alt="Logo Alpha Education" class="w-48 mx-auto"> <!-- Logo -->
        </div>
        <ul class="space-y-4">
            <li><button id="dashboardBtn" class="block hover:text-gray-200">📊 Tableau de Bord</button></li>
            <li><button id="coursBtn" class="block hover:text-gray-200">📚 Mes Cours</button></li>
            <li><button id="etudiantsBtn" class="block hover:text-gray-200">👥 Étudiants</button></li>
            <li><button id="statistiquesBtn" class="block hover:text-gray-200">📈 Statistiques</button></li>
            <li><a href="#" class="block hover:text-gray-200">🔒 Déconnexion</a></li>
        </ul>
    </aside>

    <!-- Main Content -->
    <main class="flex-1 p-6">
        <!-- Interface Tableau de Bord -->
        <div id="dashboardInterface">
            <h1 class="text-2xl font-bold mb-8">Tableau de Bord</h1>
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                <!-- Carte : Nombre de Cours -->
                <div class="bg-white rounded-lg shadow-lg p-6">
                    <h3 class="text-lg font-bold mb-2">Nombre de Cours</h3>
                    <p class="text-3xl font-bold text-lime-500">12</p>
                </div>
                <!-- Carte : Nombre d'Étudiants -->
                <div class="bg-white rounded-lg shadow-lg p-6">
                    <h3 class="text-lg font-bold mb-2">Nombre d'Étudiants</h3>
                    <p class="text-3xl font-bold text-lime-500">45</p>
                </div>
                <!-- Carte : Taux de Réussite -->
                <div class="bg-white rounded-lg shadow-lg p-6">
                    <h3 class="text-lg font-bold mb-2">Taux de Réussite</h3>
                    <p class="text-3xl font-bold text-lime-500">85%</p>
                </div>
            </div>
        </div>

        <!-- Interface Mes Cours -->
        <div id="coursInterface" class="hidden">
            <h1 class="text-2xl font-bold mb-8">Mes Cours</h1>
            <div class="mb-6">
                <button id="addCoursBtn" class="bg-orange-500 text-white px-4 py-2 rounded-lg hover:bg-orange-600">
                    + Ajouter un Cours
                </button>
            </div>
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                <!-- Cours 1 -->
                <div class="bg-white rounded-lg shadow-lg p-6">
                    <h3 class="text-lg font-bold mb-2">Mathématiques</h3>
                    <p class="text-gray-600 mb-4">Chapitres : 5</p>
                    <div class="flex items-center justify-between">
                        <button class="bg-blue-500 text-white px-2 py-1 rounded text-sm hover:bg-blue-600" onclick="afficherChapitres('Mathématiques')">Voir les Chapitres</button>
                        <button class="bg-red-500 text-white px-2 py-1 rounded text-sm hover:bg-red-600">Supprimer</button>
                    </div>
                </div>
                <!-- Cours 2 -->
                <div class="bg-white rounded-lg shadow-lg p-6">
                    <h3 class="text-lg font-bold mb-2">Physique</h3>
                    <p class="text-gray-600 mb-4">Chapitres : 3</p>
                    <div class="flex items-center justify-between">
                        <button class="bg-blue-500 text-white px-2 py-1 rounded text-sm hover:bg-blue-600" onclick="afficherChapitres('Physique')">Voir les Chapitres</button>
                        <button class="bg-red-500 text-white px-2 py-1 rounded text-sm hover:bg-red-600">Supprimer</button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Interface Chapitres -->
        <div id="chapitresInterface" class="hidden">
            <h1 class="text-2xl font-bold mb-8" id="chapitresTitre"></h1>
            <div class="mb-6">
                <button id="addChapitreBtn" class="bg-green-500 text-white px-4 py-2 rounded-lg hover:bg-green-600">
                    + Ajouter un Chapitre
                </button>
                <button onclick="retourCours()" class="bg-gray-500 text-white px-4 py-2 rounded-lg hover:bg-gray-600 ml-4">
                    ← Retour aux Cours
                </button>
            </div>
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                <!-- Chapitre 1 -->
                <div class="bg-white rounded-lg shadow-lg p-6">
                    <h3 class="text-lg font-bold mb-2">Chapitre 1 : Introduction</h3>
                    <p class="text-gray-600 mb-4">Leçons : 4</p>
                    <div class="flex items-center justify-between">
                        <button class="bg-blue-500 text-white px-2 py-1 rounded text-sm hover:bg-blue-600">Modifier</button>
                        <button class="bg-red-500 text-white px-2 py-1 rounded text-sm hover:bg-red-600">Supprimer</button>
                    </div>
                </div>
                <!-- Chapitre 2 -->
                <div class="bg-white rounded-lg shadow-lg p-6">
                    <h3 class="text-lg font-bold mb-2">Chapitre 2 : Algèbre</h3>
                    <p class="text-gray-600 mb-4">Leçons : 6</p>
                    <div class="flex items-center justify-between">
                        <button class="bg-blue-500 text-white px-2 py-1 rounded text-sm hover:bg-blue-600">Modifier</button>
                        <button class="bg-red-500 text-white px-2 py-1 rounded text-sm hover:bg-red-600">Supprimer</button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Interface Étudiants -->
        <div id="etudiantsInterface" class="hidden">
            <h1 class="text-2xl font-bold mb-8">Gestion des Étudiants</h1>
            <div class="mb-6">
                <button id="addEtudiantBtn" class="bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600">
                    + Ajouter un Étudiant
                </button>
            </div>
            <div class="bg-white rounded-lg shadow-lg p-6">
                <table class="w-full">
                    <thead>
                        <tr>
                            <th class="text-left p-2">Nom</th>
                            <th class="text-left p-2">Email</th>
                            <th class="text-left p-2">Cours Suivis</th>
                            <th class="text-left p-2">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td class="p-2">Jean Dupont</td>
                            <td class="p-2">jean.dupont@example.com</td>
                            <td class="p-2">Mathématiques, Physique</td>
                            <td class="p-2">
                                <button class="bg-blue-500 text-white px-2 py-1 rounded text-sm hover:bg-blue-600">Modifier</button>
                                <button class="bg-red-500 text-white px-2 py-1 rounded text-sm hover:bg-red-600">Supprimer</button>
                            </td>
                        </tr>
                        <tr>
                            <td class="p-2">Marie Curie</td>
                            <td class="p-2">marie.curie@example.com</td>
                            <td class="p-2">Physique</td>
                            <td class="p-2">
                                <button class="bg-blue-500 text-white px-2 py-1 rounded text-sm hover:bg-blue-600">Modifier</button>
                                <button class="bg-red-500 text-white px-2 py-1 rounded text-sm hover:bg-red-600">Supprimer</button>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Interface Statistiques -->
        <div id="statistiquesInterface" class="hidden">
            <h1 class="text-2xl font-bold mb-8">Statistiques</h1>
            <div class="bg-white rounded-lg shadow-lg p-6">
                <h3 class="text-lg font-bold mb-4">Taux de Réussite par Cours</h3>
                <div class="h-64 bg-gray-100 rounded-lg flex items-center justify-center">
                    <!-- Graphique (à implémenter) -->
                    <p class="text-gray-500">Graphique des statistiques</p>
                </div>
            </div>
        </div>
    </main>

    <!-- Modales -->
    <!-- Modal pour Ajouter un Cours -->
    <div id="addCoursModal" class="hidden fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto h-full w-full">
        <div class="relative top-20 mx-auto p-5 border w-96 shadow-lg rounded-md bg-white">
            <div class="mt-3 text-center">
                <h3 class="text-lg leading-6 font-medium text-gray-900">Ajouter un Cours</h3>
                <div class="mt-2 px-7 py-3">
                    <input type="text" id="coursName" class="w-full p-2 border border-gray-300 rounded-lg" placeholder="Nom du Cours">
                </div>
                <div class="items-center px-4 py-3">
                    <button id="saveCoursBtn" class="px-4 py-2 bg-green-500 text-white text-base font-medium rounded-md shadow-sm hover:bg-green-600 focus:outline-none focus:ring-2 focus:ring-green-300">
                        Enregistrer
                    </button>
                    <button id="closeCoursModal" class="ml-3 px-4 py-2 bg-gray-500 text-white text-base font-medium rounded-md shadow-sm hover:bg-gray-600 focus:outline-none focus:ring-2 focus:ring-gray-300">
                        Annuler
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal pour Ajouter un Étudiant -->
    <div id="addEtudiantModal" class="hidden fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto h-full w-full">
        <div class="relative top-20 mx-auto p-5 border w-96 shadow-lg rounded-md bg-white">
            <div class="mt-3 text-center">
                <h3 class="text-lg leading-6 font-medium text-gray-900">Ajouter un Étudiant</h3>
                <div class="mt-2 px-7 py-3">
                    <input type="text" id="etudiantName" class="w-full p-2 border border-gray-300 rounded-lg" placeholder="Nom de l'Étudiant">
                    <input type="email" id="etudiantEmail" class="w-full p-2 border border-gray-300 rounded-lg mt-3" placeholder="Email de l'Étudiant">
                </div>
                <div class="items-center px-4 py-3">
                    <button id="saveEtudiantBtn" class="px-4 py-2 bg-green-500 text-white text-base font-medium rounded-md shadow-sm hover:bg-green-600 focus:outline-none focus:ring-2 focus:ring-green-300">
                        Enregistrer
                    </button>
                    <button id="closeEtudiantModal" class="ml-3 px-4 py-2 bg-gray-500 text-white text-base font-medium rounded-md shadow-sm hover:bg-gray-600 focus:outline-none focus:ring-2 focus:ring-gray-300">
                        Annuler
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/1.6.5/flowbite.min.js"></script>
    <script>
        // Gestion des interfaces
        const dashboardBtn = document.getElementById('dashboardBtn');
        const coursBtn = document.getElementById('coursBtn');
        const etudiantsBtn = document.getElementById('etudiantsBtn');
        const statistiquesBtn = document.getElementById('statistiquesBtn');

        const dashboardInterface = document.getElementById('dashboardInterface');
        const coursInterface = document.getElementById('coursInterface');
        const chapitresInterface = document.getElementById('chapitresInterface');
        const etudiantsInterface = document.getElementById('etudiantsInterface');
        const statistiquesInterface = document.getElementById('statistiquesInterface');

        // Afficher l'interface Tableau de Bord
        dashboardBtn.addEventListener('click', () => {
            dashboardInterface.classList.remove('hidden');
            coursInterface.classList.add('hidden');
            chapitresInterface.classList.add('hidden');
            etudiantsInterface.classList.add('hidden');
            statistiquesInterface.classList.add('hidden');
        });

        // Afficher l'interface Mes Cours
        coursBtn.addEventListener('click', () => {
            coursInterface.classList.remove('hidden');
            dashboardInterface.classList.add('hidden');
            chapitresInterface.classList.add('hidden');
            etudiantsInterface.classList.add('hidden');
            statistiquesInterface.classList.add('hidden');
        });

        // Afficher l'interface Étudiants
        etudiantsBtn.addEventListener('click', () => {
            etudiantsInterface.classList.remove('hidden');
            dashboardInterface.classList.add('hidden');
            coursInterface.classList.add('hidden');
            chapitresInterface.classList.add('hidden');
            statistiquesInterface.classList.add('hidden');
        });

        // Afficher l'interface Statistiques
        statistiquesBtn.addEventListener('click', () => {
            statistiquesInterface.classList.remove('hidden');
            dashboardInterface.classList.add('hidden');
            coursInterface.classList.add('hidden');
            chapitresInterface.classList.add('hidden');
            etudiantsInterface.classList.add('hidden');
        });

        // Afficher l'interface Chapitres
        function afficherChapitres(cours) {
            document.getElementById('chapitresTitre').textContent = `Chapitres pour ${cours}`;
            coursInterface.classList.add('hidden');
            chapitresInterface.classList.remove('hidden');
        }

        // Retour à l'interface Cours
        function retourCours() {
            chapitresInterface.classList.add('hidden');
            coursInterface.classList.remove('hidden');
        }

        // Gestion des modales
        const addCoursBtn = document.getElementById('addCoursBtn');
        const addCoursModal = document.getElementById('addCoursModal');
        const closeCoursModal = document.getElementById('closeCoursModal');
        const saveCoursBtn = document.getElementById('saveCoursBtn');

        const addEtudiantBtn = document.getElementById('addEtudiantBtn');
        const addEtudiantModal = document.getElementById('addEtudiantModal');
        const closeEtudiantModal = document.getElementById('closeEtudiantModal');
        const saveEtudiantBtn = document.getElementById('saveEtudiantBtn');

        // Ouvrir la modal pour ajouter un cours
        addCoursBtn.addEventListener('click', () => {
            addCoursModal.classList.remove('hidden');
        });

        // Fermer la modal pour ajouter un cours
        closeCoursModal.addEventListener('click', () => {
            addCoursModal.classList.add('hidden');
        });

        // Enregistrer un cours
        saveCoursBtn.addEventListener('click', () => {
            const coursName = document.getElementById('coursName').value;
            alert(`Cours "${coursName}" enregistré !`);
            addCoursModal.classList.add('hidden');
        });

        // Ouvrir la modal pour ajouter un étudiant
        addEtudiantBtn.addEventListener('click', () => {
            addEtudiantModal.classList.remove('hidden');
        });

        // Fermer la modal pour ajouter un étudiant
        closeEtudiantModal.addEventListener('click', () => {
            addEtudiantModal.classList.add('hidden');
        });

        // Enregistrer un étudiant
        saveEtudiantBtn.addEventListener('click', () => {
            const etudiantName = document.getElementById('etudiantName').value;
            const etudiantEmail = document.getElementById('etudiantEmail').value;
            alert(`Étudiant "${etudiantName}" (${etudiantEmail}) enregistré !`);
            addEtudiantModal.classList.add('hidden');
        });
    </script>
</body>
</html>