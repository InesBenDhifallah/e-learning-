<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Questions du Quiz</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex bg-gray-100">
    <main class="flex-1 p-6">
        <h1 class="text-2xl font-bold">Quiz : {{ quiz.matiere }}</h1>

        <div class="mt-6">
            {% for question in questions %}
                <div class="bg-white p-4 shadow-lg rounded-lg mb-4">
                    <h2 class="text-lg font-bold">{{ question.question }}</h2>
                    <p class="text-gray-600">Réponses :</p>
                    <ul class="list-disc ml-6">
                        <li>{{ question.reponse }}</li>
                    </ul>
                </div>
            {% else %}
                <p class="text-gray-500">Aucune question disponible.</p>
            {% endfor %}
        </div>

        <a href="{{ path('quizz_index') }}" class="bg-gray-500 text-white px-4 py-2 rounded-lg mt-4 inline-block">Retour aux Quiz</a>
    </main>
</body>
</html>
