<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Alpha Education{% endblock %}</title>
    
    <!-- Importation de TailwindCSS & Flowbite -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/1.6.5/flowbite.min.css">
    
    <!-- Ajout de Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    
    <style>
        body {
            font-family: 'Poppins', sans-serif;
        }
    </style>
</head>
<body class="flex bg-gray-100">

    <!-- Sidebar -->
    <aside class="w-64 bg-lime-500 text-white p-5 min-h-screen shadow-lg">
        <div class="text-2xl font-bold mb-6 text-center">Alpha Education</div>
        <ul class="space-y-4">
            <li><a href="#" class="block px-3 py-2 rounded-lg hover:bg-lime-600 transition">👤 Profil</a></li>
            <li><a href="#" class="block px-3 py-2 rounded-lg hover:bg-lime-600 transition">📚 Cours</a></li>
            <li><a href="#" class="block px-3 py-2 rounded-lg hover:bg-lime-600 transition">📘 Chapitres</a></li>
            <li><a href="#" class="block px-3 py-2 rounded-lg hover:bg-lime-600 transition">📝 Blog</a></li>
            <li><a href="#" class="block px-3 py-2 rounded-lg hover:bg-red-600 transition">🔒 Déconnexion</a></li>
        </ul>
    </aside>

    <!-- Contenu principal -->
    <main class="flex-1 p-6">
        <!-- Messages Flash -->
        {% for message in app.flashes('success') %}
            <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4 flex items-center justify-between" role="alert">
                <span>{{ message }}</span>
                <button class="text-green-600 hover:text-green-800 font-bold text-lg" onclick="this.parentElement.remove();">&times;</button>
            </div>
        {% endfor %}

        {% for message in app.flashes('error') %}
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4 flex items-center justify-between" role="alert">
                <span>{{ message }}</span>
                <button class="text-red-600 hover:text-red-800 font-bold text-lg" onclick="this.parentElement.remove();">&times;</button>
            </div>
        {% endfor %}

        <!-- Contenu dynamique de la page -->
        <div class="bg-white p-6 shadow-md rounded-lg">
            {% block body %}{% endblock %}
        </div>
    </main>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/1.6.5/flowbite.min.js"></script>
</body>
</html>
