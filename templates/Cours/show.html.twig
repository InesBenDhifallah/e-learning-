{% extends 'sidebarprof.html.twig' %}

{% block title %}Cours{% endblock %}

{% block body %}
    <h1>Cours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cour.id }}</td>
            </tr>
            <tr>
                <th>Nomcours</th>
                <td>{{ cour.nomcours }}</td>
            </tr>
            <tr>
                <th>Matierecours</th>
                <td>{{ cour.matierecours }}</td>
            </tr>
            <tr>
                <th>Niveaucours</th>
                <td>{{ cour.niveaucours }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cours_index') }}">back to list</a>

    <a href="{{ path('app_cours_edit', {'id': cour.id}) }}">edit</a>

    {{ include('cours/_delete_form.html.twig') }}
{% endblock %}
